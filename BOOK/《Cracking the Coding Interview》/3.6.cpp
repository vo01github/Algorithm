

// 8.3 栈与队列


// __________________________ 3.6 __________________________
/*
	写程序将一个栈按升序排序（即最大元素位于栈顶）。
	最多只能使用一个额外的的栈存放临时数据，但不得将元素复制到别的数据结构中（如数组）
	该栈 支持的 操作有：push | pop | peek 【返回位于 Stack 顶部的对象但不将其移除。】 | isEmpty。
*/


